<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/index.css">
    <title>URL Shortner</title>
</head>

<body>
    <header>
        <h2>URL shortner</h2>
    </header>

    <form method="POST" action="/url">
        <label for="url">Enter original URL: </label>
        <input type="text" name="url" id="url" />
        <button type="submit">Generate</button>
    </form>
    <% if (locals.id) { %>
        <p> YOUR Generated URL : <a target="_blank" href="http://localhost/url/<%= id %>">http://localhost/url/<%= id %>
                    </a>
        </p>
        <% } %>
            <% if (locals.url) { %>

                <table>
                    <thead>
                        <th>sr.no.</th>
                        <th>shotid</th>
                        <th>redirect url</th>
                        <th>click count</th>
                    </thead>
                    <tbody>
                        <% url.forEach((url,index)=>{ %>
                            <tr>
                                <td>
                                    <%= index +1 %>
                                </td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td>
                                    <%= url.redirectUrl %>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>

                            <% }) %>
                    </tbody>
                </table>

                <% } %>
</body>

</html>